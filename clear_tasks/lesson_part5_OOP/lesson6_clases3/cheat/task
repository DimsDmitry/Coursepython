Создайте класс для представления автомобиля с атрибутами «марка», «модель» и «год выпуска». Реализуйте методы для получения информации об автомобиле (например, метод get_info()) и для изменения его состояния (например, метод set_year()).
Создайте класс для представления сотрудника с атрибутами «имя», «зарплата» и «должность». Реализуйте методы для получения информации о сотруднике (например, метод get_info()) и для изменения его данных (например, метод set_salary()).
Создайте класс для представления книги с атрибутами «автор», «название» и «год издания». Реализуйте методы для получения информации о книге (например, метод get_info()) и для изменения её данных (например, метод set_year()).
Создайте класс для представления музыкального альбома с атрибутами «исполнитель», «альбом», «жанр» и «год выпуска». Реализуйте методы для получения информации об альбоме (например, метод get_info()) и для изменения его данных (например, метод set_year()).
Создайте класс для представления геометрической фигуры с атрибутами «тип фигуры» (круг, квадрат, треугольник) и «размеры». Реализуйте методы для получения информации о фигуре (например, метод get_info()) и для изменения её размеров (например, метод set_size()).